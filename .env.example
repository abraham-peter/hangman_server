# Example env vars for local development / Render
SQLALCHEMY_DATABASE_URL=postgres://<user>:<pass>@<host>:<port>/<db>
REDIS_URL=redis://<user>:<pass>@<host>:<port>
SECRET_KEY=changeme
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Optional (local fallback uses sqlite if not set)
# SQLALCHEMY_DATABASE_URL=sqlite:///./test.db